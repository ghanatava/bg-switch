---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: progressivedeployments.apps.my.domain
spec:
  group: apps.my.domain
  names:
    kind: ProgressiveDeployment
    listKind: ProgressiveDeploymentList
    plural: progressivedeployments
    shortNames:
    - pd
    singular: progressivedeployment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.currentStep
      name: Step
      type: integer
    - jsonPath: .status.canaryPercentage
      name: Canary%
      type: integer
    - jsonPath: .status.healthStatus
      name: Health
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProgressiveDeployment is the Schema for the progressivedeployments
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ProgressiveDeployment
            properties:
              autoPromote:
                type: boolean
              canarySteps:
                items:
                  type: integer
                type: array
              metrics:
                description: MetricsConfig Custom type
                properties:
                  errorRate:
                    description: MetricThreshold defines a metric query and its threshold
                    properties:
                      query:
                        description: Query is the PromQL query to execute
                        type: string
                      threshold:
                        description: Threshold is the maximum acceptable value
                        type: number
                    required:
                    - query
                    - threshold
                    type: object
                  latency:
                    description: MetricThreshold defines a metric query and its threshold
                    properties:
                      query:
                        description: Query is the PromQL query to execute
                        type: string
                      threshold:
                        description: Threshold is the maximum acceptable value
                        type: number
                    required:
                    - query
                    - threshold
                    type: object
                  prometheusUrl:
                    description: PrometheusURL is the Prometheus endpoint (optional,
                      defaults to in-cluster)
                    type: string
                type: object
              stepDuration:
                type: string
              targetDeployment:
                description: foo is an example field of ProgressiveDeployment. Edit
                  progressivedeployment_types.go to remove/update
                type: string
            required:
            - autoPromote
            - canarySteps
            - metrics
            - stepDuration
            type: object
          status:
            description: status defines the observed state of ProgressiveDeployment
            properties:
              canaryDeployment:
                description: CanaryDeployment is the name of the canary Deployment
                type: string
              canaryPercentage:
                description: CanaryPercentage is the current traffic percentage going
                  to canary
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentStep:
                description: CurrentStep is the current canary step index (0-based)
                type: integer
              healthStatus:
                description: HealthStatus indicates if the canary is healthy
                enum:
                - Healthy
                - Unhealthy
                - Unknown
                type: string
              metrics:
                additionalProperties:
                  type: number
                description: Metrics contains the last observed metric values
                type: object
              phase:
                description: |-
                  conditions represent the current state of the ProgressiveDeployment resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                enum:
                - Initializing
                - Analyzing
                - Promoting
                - RollingBack
                - Completed
                - RolledBack
                - Failed
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
